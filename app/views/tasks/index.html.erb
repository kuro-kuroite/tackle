<div class="row">
  <div class="col">
    <h1>All Tasks</h1>
    <% @tasks.each do |task| %>
      <%= render task, task: task, comment: task.comments.last %>
    <% end %>
    <%= render 'form', task: @task %>

    <%= paginate @tasks %>
  </div>
</div>
