<div class="container">
  <div class="row">
    <div class="">
      <h1>Show Task</h1>
      <%= render @task %>
    </div>
  </div>
  <%= render @comments %>
  <%= render 'comments/form', task: @task, comment: @comment %>
</div>
